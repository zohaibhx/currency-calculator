<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Local Currency Converter</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body class="bg-light">
    <div class="container py-5">
      <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
          <div class="card shadow-sm">
            <div class="card-body">
              <h3 class="card-title mb-3">Currency Converter (Localhost)</h3>

              <form method="POST" action="{{ url_for('index') }}">
                <div class="mb-3">
                  <label for="amount" class="form-label">Amount</label>
                  <input type="text" name="amount" id="amount" class="form-control" placeholder="e.g. 100" value="{{ amount|default('') }}" required>
                </div>

                <div class="row">
                  <div class="col-md-6 mb-3">
                    <label for="from_curr" class="form-label">From</label>
                    <select name="from_curr" id="from_curr" class="form-select">
                      {% for c in currencies %}
                      <option value="{{ c }}" {% if from_curr == c %}selected{% endif %}>{{ c }}</option>
                      {% endfor %}
                    </select>
                  </div>

                  <div class="col-md-6 mb-3">
                    <label for="to_curr" class="form-label">To</label>
                    <select name="to_curr" id="to_curr" class="form-select">
                      {% for c in currencies %}
                      <option value="{{ c }}" {% if to_curr == c %}selected{% endif %}>{{ c }}</option>
                      {% endfor %}
                    </select>
                  </div>
                </div>

                <div class="d-grid gap-2 d-md-block">
                  <button type="submit" class="btn btn-primary">Convert</button>
                  <a href="{{ url_for('index') }}" class="btn btn-outline-secondary">Reset</a>
                </div>
              </form>

              <hr>

              
              
        </div>
      </div>
    </div>
  </body>
</html
